@using project_c.Models;
@using Microsoft.AspNetCore.Http;
@using project_c.Controllers;

@{
    ViewData["Title"] = "Index";
}

<h2>Winkelwagen</h2>

@{
    if (ViewBag.Contents != null)
    {
        <table border="1">
            <tr>
                <th>Titel</th>
                <th>Prijs</th>
                <th>Subtotaal</th>
            </tr>
            @foreach (var cartItem in ViewBag.Contents)
            {
                <tr>
                    <td>@cartItem.Title</td>
                    <td>@cartItem.Price</td>
                    <td>@(string.Format("{0:N0}", cartItem.Price * 1))</td>
                    <td>
                        @Html.ActionLink("Verwijderen", "Delete", "ShoppingCart", new { id = cartItem.Id }, new { cartItem.FindIndex }, null)
                    </td>
                </tr>
            }
            <tr>
                <td colspan="3">Totaal:</td>
                <td>
                    @{
                        List<Game> temp = ViewBag.Contents;
                        var total = String.Format("{0:N0}", temp.Sum(x => x.Price));
                    }
                    @total  EUR
                </td>

            </tr>

        </table>

        <br />
        @Html.ActionLink("Doorgaan met winkelen", "Index", "Games")
        <br />
        <br />

        <br />
        @*@Html.ActionLink("Doorgaan", "UserDetailsPage", "Order")*@
    }
    else
    {
        <br />
        <h4>Winkelwagen is leeg</h4>
    }
}

